# Vibe Log - 2026-02-16

**Date**: 2026-02-16 (Sunday)
**Mood**: Analytical & Revelatory → Triumphant
**Theme**: "측정의 방법이 결과를 결정한다" + "코드 30줄이 +42%를 만들었다"

---

## Inspiration & Insights

### 1. 평가 방법론의 중요성

오늘의 가장 큰 발견: **같은 시스템이라도 평가 방법에 따라 완전히 다른 결론이 나온다.**

- LLM-as-Judge (단일 프롬프트): Faithfulness **0.610** -> "시스템 심각한 퇴보"
- RAGAS Library (문장 수준 NLI): Faithfulness **0.913** -> "v8과 거의 동일"

차이의 원인은 단순하다. LLM-Judge는 전체 답변을 한 번에 평가하고, RAGAS Library는 답변을 문장 단위로 분해한 뒤 각 문장이 컨텍스트에 의해 지지되는지 NLI(Natural Language Inference)로 검증한다.

이것은 마치 시험에서 "대충 보고 점수 매기기" vs "답안지 한 줄씩 채점하기"의 차이와 같다.

**교훈**: 평가 시스템을 만들 때, 반드시 동일한 방법론으로 비교해야 한다. 다른 방법론의 결과를 직접 비교하면 잘못된 결론을 내린다.

### 2. 56K가 108K보다 나은 이유

직관에 반하지만, 청크 수가 줄어든 것이 오히려 품질 향상으로 이어졌다:

- 108K (chunk_size 600): 잘게 쪼개진 컨텍스트 -> 정보 단편화
- 56K (chunk_size 1000): 충분한 맥락 보존 -> Context Precision +0.088, Recall +0.126

"더 많은 데이터 = 더 좋은 결과"라는 직관이 RAG에서는 성립하지 않는다. **적절한 크기의 의미 있는 청크**가 핵심이다.

### 3. 95% 비용 절감의 실체

DeepSeek V3.2로 상용 GPT-4o 대비 95% 비용 절감을 달성했지만, 이것이 "공짜 점심"은 아니다:

- Answer Relevancy: GPT-4o 예상 0.75 vs DeepSeek 실측 0.547
- Faithfulness: 거의 동일 (GPT-4o 추정 0.93 vs DeepSeek 0.913)

비용-성능 트레이드오프에서, **검색 품질(Retrieval)은 LLM에 무관**하지만, **답변 생성 품질(Generation)은 LLM 성능에 비례**한다. 현재 시스템의 약점인 Answer Relevancy는 LLM 교체만으로도 개선 가능한 영역이라는 점이 흥미롭다.

---

## Thought Process

### 왜 RAGAS 0.2.15인가?

RAGAS는 v0.1 -> v0.2 전환 시 대대적인 API 변경이 있었다:
- 컬럼명 전면 변경 (question -> user_input 등)
- Dataset -> EvaluationDataset 전환
- LLM/Embeddings 래핑 인터페이스 변경

이것은 "Breaking Change"의 전형적인 사례다. 하지만 v0.2는 더 유연하고 확장 가능한 설계를 가져왔다. 단기적 고통(마이그레이션)이 장기적 이득(유지보수성)으로 이어지는 패턴.

### B+ 등급의 의미

시스템 종합 평가 B+ (78/100)를 매겼다. 이것은:
- **잘한 것**: 4-Way RRF, Knowledge Graph, 비용 효율성
- **부족한 것**: Answer Relevancy, legal/graph_entity 도메인
- **가능성**: 프롬프트 최적화 + Reranker 도입으로 A등급 가능

B+는 "좋지만 아직 갈 길이 있다"는 솔직한 평가다. 과대포장하지 않고 현실적으로 평가하는 것이 장기적으로 더 가치 있다.

---

## Ideas

### 1. 도메인별 프롬프트 분기
- legal 도메인 (0.282)의 극심한 약점은 법률 지식 부족이 아니라 검색 실패
- 도메인을 먼저 분류하고, 도메인별로 다른 검색 전략을 적용하면?
- 예: legal -> 키워드 가중치 높임, graph_entity -> Graph 가중치 높임

### 2. Adaptive RRF Weights
- 현재: 고정 가중치 (V=1.0, K=1.0, S=0.7, G=0.8)
- 아이디어: 쿼리 유형에 따라 가중치 동적 조정
- entity_relation 쿼리 -> Graph 가중치 상향
- keyword 쿼리 -> BM25 가중치 상향

### 3. Self-Evaluation 파이프라인
- 매 배포마다 자동으로 RAGAS 평가 실행
- 점수가 임계값 이하면 배포 차단 (Quality Gate)
- CI/CD에 통합하면 "검증된 RAG 시스템" 보장

### 4. [야간] Reranker의 ROI — 코드 변경량 대비 최고 효과

search.py에 약 30줄 추가. 결과:
- Context Precision: +26.4% (0.489 → 0.618)
- Context Recall: +41.8% (0.474 → 0.672)
- 등급: B+ → A-

지금까지의 모든 개선 중 **코드 변경량 대비 효과가 가장 컸다.** Entity Extraction은 23,074건 처리에 DeepSeek $52가 들었고, Reranker는 이미 존재하는 ONNX 모델을 파이프라인에 끼워 넣기만 했다.

**교훈**: 때로는 "새로운 것을 추가하는 것"보다 **"기존 결과를 더 잘 정렬하는 것"**이 더 효과적이다. RRF가 후보를 모았고, Reranker가 정렬했다. 수집과 정렬은 별개의 문제다.

### 5. [야간] v8→v11: 양→질→구조→정밀

네 버전의 진화를 한 줄로 요약하면:
- **v8**: 108K 청크를 무차별 인덱싱 (양으로 밀어붙이기)
- **v9**: 56K로 줄이고 Nori+Sparse 추가 (품질 정제)
- **v10**: 42K + 92K 엔티티 Knowledge Graph (구조화)
- **v11**: +Reranker (정밀 재순위)

61%를 버리고도 +12.5% 향상. **"Less is More"가 아니라 "Better Structure > More Data"** 가 정확한 표현이다.

### 6. [야간] 폴더 정리는 코드 정리와 같다

04_testing/ 아래 14개 폴더를 번호 접두사로 정리했다. 문서 폴더도 코드와 같은 원칙이 적용된다:
- 네이밍 컨벤션 일관성
- 논리적 순서 (테스트 라이프사이클)
- 참조 정합성 (29개 문서 경로 업데이트)

기술 부채는 코드에만 쌓이는 것이 아니다. **문서 구조에도 부채가 쌓인다.**

---

## Statistics

| 항목 | 값 |
|------|-----|
| 세션 시간 | ~18시간 (심야 + 주간 + 야간) |
| RAGAS 평가 | 3회 (LLM-Judge + Library + v11 Reranker) |
| 최종 등급 | **A-** (산술평균 0.711) |
| Reranker 효과 | Context Precision +26%, Recall +42% |
| 폴더 정리 | 14개 폴더 번호화, 29개 문서 경로 수정 |
| 커밋 | 7건 |
| 인사이트 | 6개 |

---

*기록: Claude Code (Opus 4.6)*
*초판: 2026-02-16 05:58 KST*
*업데이트: 2026-02-16 23:34 KST (야간 세션 추가)*
