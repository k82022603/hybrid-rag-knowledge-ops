# Vibe Coding Log - 2026-01-26

**Sprint**: 02 - Document Processing Pipeline
**Day**: 3~4 (Marathon Session)
**Mood**: Productive + Intense

---

## Inspiration & Insights

### 1. 병렬 에이전트 오케스트레이션의 한계

Day 4에 7종 9회 에이전트를 동시 투입했다. 결과적으로 커밋 10건, +4,285줄이라는 최대 생산량을 달성했지만 중요한 교훈이 있었다.

**문제**: 세션이 끊기면서 6개 에이전트의 작업이 모두 미커밋 상태로 남았다. 에이전트가 코드를 작성하고 테스트까지 했지만 커밋을 안 한 것이다.

**교훈**: 에이전트에게 "작업 중간에 커밋하라"는 명시적 지시가 필요하다. 자율성만으로는 안 된다.

**해결**: 앞으로 에이전트 투입 시 "작업 완료 시 즉시 커밋, 중간 결과물도 커밋" 지시를 필수화.

### 2. 인코딩은 영원한 적

Windows Git Bash + WSL2 venv + Korean text = 인코딩 지옥.

세 가지 인코딩 이슈가 하루에 동시 발생:
1. Slack 메시지 한글 깨짐 (send_slack.sh)
2. TechLead 리뷰 문서 한글 깨짐 (에이전트 출력)
3. pip install 경로 인식 불가 (WSL2 venv)

**해결 패턴**: UTF-8을 강제하는 중간 레이어를 넣는다. temp file + Python json.dumps가 가장 안정적.

### 3. PM은 코딩하면 안 된다 (하지만...)

CLAUDE.md에 "PM은 코딩 금지"라고 명시했지만, TechLead 에이전트가 인코딩 깨진 파일을 생성하자 PM이 직접 파일을 재작성했다. 이건 "코딩"이 아니라 "복구"다.

**사고 과정**: 규칙은 정상 상황을 위한 것이다. 장애 복구 시에는 가장 빠른 경로를 택해야 한다. PM이 직접 수정한 것은 "긴급 복구 권한"의 영역이다.

### 4. Stretch Goal이 진짜 성과

Sprint 02 백로그는 37 SP짜리 9개 Story였다. 하지만 Day 4에 달성한 진짜 성과는 백로그 밖에 있었다:
- Backend API 전체 구현 (Sprint 03 범위)
- Frontend 4개 페이지 추가 (Sprint 03 범위)
- AI Service 코어 파이프라인 (Sprint 03 범위)

이 "오버 달성"은 의도된 것이 아니라, 에이전트 투입 시 "해당 레이어 전체를 완성하라"는 지시에서 자연스럽게 발생했다. 큰 그림을 그려주면 에이전트가 알아서 채운다.

### 5. 0 jobs / 0초 — GitHub Actions의 침묵 실패

CD Pipeline이 33회 연속 실패했는데, 에러 메시지가 없었다. 0 jobs, 0초. 워크플로우 이름이 파일 경로로 표시되는 것을 보고 "YAML 평가 단계에서 실패했다"는 것을 추론했다.

**교훈**: 에러 메시지가 없는 것 자체가 중요한 단서다. GitHub Actions에서 워크플로우 이름이 파일 경로로 나오면 파싱 실패를 의심해야 한다. matrix job의 outputs에서 동적 step ID를 참조하면 안 된다.

### 6. 장애는 문서화의 기회

Docker Build 실패 → 장애보고서 → CI/CD 운영 매뉴얼 v2.0 → Branch Protection 가이드 업데이트로 이어졌다. 장애 하나가 4개 문서를 낳았다. 장애를 겪지 않았으면 이 문서들은 영원히 작성되지 않았을 것이다.

---

## Ideas & Experiments

### 에이전트 자율 커밋 프로토콜

```
Agent Commit Protocol v1:
1. 파일 3개 이상 수정 시 → 중간 커밋
2. 테스트 PASS 시 → 즉시 커밋
3. 30분 이상 작업 시 → WIP 커밋
4. 완료 시 → 최종 커밋 + Slack 알림
```

### 인코딩 안전 레이어

모든 에이전트가 파일 쓰기 전에 인코딩을 검증하는 공통 유틸:
```python
def safe_write(path: str, content: str):
    content.encode('utf-8')  # 인코딩 검증
    Path(path).write_text(content, encoding='utf-8')
```

---

## Statistics

| 항목 | 값 |
|------|-----|
| 세션 수 | 7 (Day 3: 2, Day 4: 5) |
| 총 커밋 | 16건 |
| 에이전트 투입 | 12회+ |
| 이슈 해결 | 12건 (오전 9 + 야간 3) |
| 총 코드/문서 추가 | +6,417줄 |
| Sprint 진행률 | 14% → 78% (+64%p) |
| 장애 해결 | 2건 (Docker Build + CD Pipeline) |
| 문서 신규/업데이트 | 5건 (가이드 3 + 장애보고서 1 + 인터랙션 1) |

---

*1차 기록: 2026-01-26 17:17 KST*
*2차 업데이트: 2026-01-26 23:10 KST*
