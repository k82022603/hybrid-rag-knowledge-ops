# STORY-111: HRKP vs RCSV Cross-System Comparison Report

**평가 일시**: 2026-02-10 08:26 KST
**평가 방법**: LLM-as-Judge (DeepSeek V3)
**테스트 쿼리**: 12개

---

## 1. 시스템 비교

| 항목 | HRKP (Graph RAG) | RCSV (RAGChatbotServer) |
|------|------------------|------------------------|
| 검색 채널 | BM25 + Dense + **Graph** | BM25 + Dense |
| 퓨전 알고리즘 | **RRF** (Reciprocal Rank Fusion) | Alpha-weighted (0.6/0.4) |
| 임베딩 모델 | **BGE-M3** (1024d, 로컬) | OpenAI text-embedding-3-small (1536d) |
| LLM | **DeepSeek V3** | GPT-4o-mini |
| Knowledge Graph | **Neo4j** (엔티티 934개, 관계 165개) | 없음 |
| 문서 수 | 13,430 청크 | 12,918 청크 (96%) |

## 2. RAGAS 메트릭 비교

| 메트릭 | HRKP | RCSV | 차이 | 우위 |
|--------|:----:|:----:|:----:|:----:|
| faithfulness | 0.0667 | 0.0667 | +0.0000 | 동일 |
| answer_relevancy | 0.4167 | 0.4833 | -0.0666 | **RCSV** |
| context_precision | 0.3667 | 0.4667 | -0.1000 | **RCSV** |
| context_recall | 0.0750 | 0.2167 | -0.1417 | **RCSV** |

## 3. 검색 성능 비교

| 지표 | HRKP | RCSV |
|------|:----:|:----:|
| 평균 레이턴시 | 6ms | 390ms |
| Top-1 Graph 출처 | 12/12 | 0/12 |

## 4. 쿼리별 답변 비교

### Q1: "Neo4j와 Elasticsearch의 역할 차이점은?" (entity_relation)

**HRKP** (388c, 9.0ms): 컨텍스트에 따르면, Neo4j는 그래프 데이터베이스(Graph DB)로, 데이터 간의 관계와 연결성을 저장 및 분석하는 데 특화되어 있습니다. 특히 RAG 시스템에서 환각(Hallucination) 개선을 위해 활용될 수 있습니다.

Elasticsearch는 하이브리드 검색 엔진으로, 정형 데이터 검색, 전문(Full-text) 검색, 벡터 검색을 모두 ...

**RCSV** (323c, 547.0ms): Neo4j는 그래프 데이터베이스로, 데이터 간의 관계를 모델링하고 탐색하는 데 최적화되어 있습니다. 주로 복잡한 관계형 데이터를 처리하고, 노드와 엣지를 통해 데이터 간의 연결성을 강조합니다.

반면, Elasticsearch는 분산 검색 엔진으로, 정형 데이터 검색, 전문 검색, 벡터 검색 기능을 제공합니다. 대규모 데이터에 대한 빠른 검색과 실시간 분석...

### Q2: "LangGraph와 LangChain 중 어떤 것을 사용해야 하나요?" (entity_relation)

**HRKP** (240c, 5.6ms): LangGraph와 LangChain 중 선택은 프로젝트의 복잡성과 필요에 따라 다릅니다.

*   **LangChain**은 선형 워크플로우나 단순한 에이전트 패턴에 적합하며, 학습 곡선이 더 낮고 구현이 쉽습니다.
*   **LangGraph**는 비선형 오케스트레이션이나 복잡한 태스크 흐름, 세밀한 제어가 필요한 유연한 워크플로우에 적합합니다.
*  ...

**RCSV** (261c, 481.9ms): LangGraph는 그래프에서 단계를 모델링하여 LLM을 사용하는 멀티액터 애플리케이션을 구축하는 데 적합하며, 상태 유지가 필요한 경우 유용합니다. 반면, LangChain은 대규모 언어 모델을 활용한 애플리케이션 개발을 위한 프레임워크로, 다양한 챗 모델과 컴포넌트를 통합하여 쉽게 애플리케이션을 구축할 수 있습니다. 따라서, 멀티액터 시스템이 필요하면 ...

### Q3: "FastAPI와 PostgreSQL을 연동하여 RAGAS 평가를 수행하려면?" (entity_relation)

**HRKP** (278c, 5.6ms): 컨텍스트에 FastAPI, PostgreSQL, RAGAS 평가에 대한 구체적인 연동 방법은 명시되어 있지 않습니다. 제공된 내용은 주로 도구 사용 에이전트, ReAct, Dynamic Planning + Execution, 그리고 LangChain, AutoGen, LangGraph와 같은 AI 에이전트 구현 프레임워크의 비교에 초점을 맞추고 있습니다.
...

**RCSV** (1219c, 408.2ms): FastAPI와 PostgreSQL을 연동하여 RAGAS 평가를 수행하려면 다음 단계를 따르면 됩니다:

1. **FastAPI 설치**: FastAPI와 필요한 라이브러리(예: `uvicorn`, `sqlalchemy`, `asyncpg`)를 설치합니다.
   
   ```bash
   pip install fastapi uvicorn sqlalchemy...

### Q4: "RAG Pipeline에서 BGE-M3 임베딩 모델의 역할은?" (multi_hop)

**HRKP** (65c, 5.5ms): 컨텍스트에 BGE-M3 임베딩 모델에 대한 언급이 없습니다. 따라서 제공된 정보로는 해당 질문에 답변할 수 없습니다....

**RCSV** (116c, 349.3ms): RAG Pipeline에서 BGE-M3 임베딩 모델의 역할은 사용자가 입력한 쿼리를 벡터 표현으로 변환하여, 관련 데이터 검색 단계에서 벡터 데이터베이스에서 유사한 데이터 조각을 효과적으로 검색하는 것입니다....

### Q5: "Kubernetes에서 Spring Boot 마이크로서비스를 배포하는 방법은?" (multi_hop)

**HRKP** (134c, 5.3ms): 컨텍스트에 Spring Boot 마이크로서비스 배포 방법에 대한 구체적인 내용이 없습니다. 제공된 컨텍스트는 Kubernetes가 마이크로서비스와 분산 애플리케이션 관리에 유용한 오케스트레이션 플랫폼이라는 일반적인 설명만 포함하고 있습니다....

**RCSV** (656c, 409.0ms): Kubernetes에서 Spring Boot 마이크로서비스를 배포하는 방법은 다음과 같습니다:

1. **Docker 이미지 생성**: Spring Boot 애플리케이션을 Docker 이미지로 패키징합니다. `Dockerfile`을 작성하고 `docker build` 명령어로 이미지를 생성합니다.

2. **Kubernetes 클러스터 설정**: Kuber...

### Q6: "Agentic AI 에이전트 워크플로우 설계 패턴은 무엇인가요?" (multi_hop)

**HRKP** (448c, 4.9ms): 컨텍스트에 따르면, Agentic AI 에이전트 워크플로우 설계 패턴은 다음과 같습니다:

1. **파이프라인 패턴**: 작업이 순차적인 단계별 흐름을 따라 각 에이전트가 단계를 처리합니다.
2. **ReAct 패턴**: 추론(Reasoning)과 행동(Action)을 번갈아 수행하여 복잡한 문제를 단계적으로 해결합니다.
3. **Supervisor-Wor...

**RCSV** (442c, 440.5ms): Agentic AI 에이전트 워크플로우 설계 패턴은 다음과 같습니다:

1. **결정론적 구조**: 예측 가능한 실행 경로를 제공하여 작업을 명확하게 정의된 하위 작업으로 분해합니다.
2. **에이전트와 워크플로우의 상호보완성**: 에이전트는 워크플로우 내에서 자율적으로 특정 과업을 수행하며, 워크플로우는 전체 프로세스를 조율합니다.
3. **데이터 흐름 ...

### Q7: "Docker Compose 설정 방법은?" (keyword)

**HRKP** (191c, 5.6ms): 컨텍스트에는 Docker Compose 설정 방법에 대한 정보가 포함되어 있지 않습니다. 제공된 내용은 Docker Desktop 환경에서 PostgreSQL 컨테이너와 MCP 서버를 설정하는 개요와 두 가지 방식(Docker 방식과 NPX 방식)을 언급하고 있지만, 구체적인 Docker Compose 설정 절차나 예시는 설명하지 않습니다....

**RCSV** (78c, 366.5ms): 컨텍스트에 Docker Compose 설정 방법에 대한 정보는 포함되어 있지 않습니다. 따라서 해당 질문에 대한 답변을 제공할 수 없습니다....

### Q8: "RRF 알고리즘이 Hybrid 검색에서 하는 역할은?" (keyword)

**HRKP** (165c, 6.3ms): RRF(Reciprocal Rank Fusion) 알고리즘은 하이브리드 검색에서 키워드 기반 검색(BM25)과 벡터 검색(KNN)과 같은 서로 다른 검색 기법의 결과를 통합하고 재순위화하는 역할을 합니다. 이를 통해 단일 검색 방식의 한계를 보완하고 최종 검색 결과의 정확성과 관련성을 높입니다....

**RCSV** (127c, 324.7ms): RRF 알고리즘은 Hybrid 검색에서 검색 결과의 순위를 조정하는 역할을 합니다. 이를 통해 키워드 기반 검색, 의미 기반 검색, 벡터 검색의 결과를 통합하여 최종적으로 더 정확하고 관련성 높은 응답을 생성하는 데 기여합니다....

### Q9: "RAGAS 평가 메트릭의 종류와 의미는?" (keyword)

**HRKP** (185c, 5.6ms): 컨텍스트에 제공된 정보에는 RAGAS 평가 메트릭의 구체적인 종류와 의미에 대한 설명이 없습니다. 컨텍스트는 RAG 시스템의 한계, 에이전트 기반 RAG의 구조, 그리고 성능 평가 시 'Generation Accuracy'를 사용하는 한 가지 예시만 언급하고 있습니다.

따라서, 주어진 컨텍스트만으로는 질문에 답변할 수 없습니다....

**RCSV** (736c, 353.3ms): RAGAS 평가 메트릭의 종류와 의미는 다음과 같습니다:

1. **테스트 정확도 (Accuracy)**: 전체 예측 중 맞춘 비율을 계산하며, 간단하고 직관적입니다. 그러나 불균형 데이터에서는 신뢰할 수 없습니다.

2. **혼동 행렬 (Confusion Matrix)**: 실제 클래스와 예측 클래스를 교차 표로 표현하여 TP, FP, FN, TN의 개수...

### Q10: "대규모 문서를 효율적으로 처리하는 방법은?" (semantic)

**HRKP** (327c, 6.8ms): 컨텍스트에 따르면, 대규모 문서를 효율적으로 처리하는 방법은 **청킹(Chunking)** 작업을 통해 문서를 작은 단위로 나누는 것입니다. 이는 다음과 같은 이유로 효율성을 높입니다:

1. **효율적인 검색**: 각 청크를 독립적으로 검색하여 관련 문서를 빠르고 정확하게 찾습니다.
2. **문맥 유지**: LLM의 입력 길이 제한 내에서 각 청크가 문맥...

**RCSV** (358c, 318.5ms): 대규모 문서를 효율적으로 처리하는 방법은 다음과 같습니다:

1. **청킹(Chunking)**: 긴 문서를 작은 청크로 나누어 각 청크를 독립적으로 검색할 수 있도록 합니다. 이를 통해 질문과 관련된 문서를 더 빠르고 정확하게 찾을 수 있습니다.

2. **문맥 유지**: LLM의 입력 길이에 제한이 있으므로, 청크 단위로 나누어 각 청크가 문맥을 유지하...

### Q11: "검색 성능을 최적화하려면 어떻게 해야 하나요?" (semantic)

**HRKP** (486c, 5.6ms): 컨텍스트에 따르면 검색 성능 최적화를 위해 다음과 같은 전략을 사용할 수 있습니다:

1. **하이브리드 검색 탐색**: 키워드 기반 검색, 의미적 검색, 벡터 검색을 지능적으로 결합합니다.
2. **재귀 검색 및 쿼리 엔진**: 작은 덩어리로 검색을 시작한 후, 점차 더 크고 맥락이 풍부한 정보를 확보하여 효율성과 응답 품질의 균형을 맞춥니다.
3. **...

**RCSV** (438c, 330.6ms): 검색 성능을 최적화하려면 다음과 같은 전략을 활용해야 합니다:

1. **하이브리드 검색 탐색**: 키워드 기반 검색, 의미적 검색, 벡터 검색을 지능적으로 결합하여 성능을 향상시킵니다.
2. **재귀 검색 및 쿼리 엔진**: 작은 덩어리로 검색을 시작하고, 더 큰 맥락 정보를 확보하여 효율성과 응답의 맥락적 풍부함을 균형 있게 맞춥니다.
3. **Step...

### Q12: "환경변수를 안전하게 관리하는 방법은?" (semantic)

**HRKP** (124c, 4.7ms): 컨텍스트에 제시된 환경변수 관리 방법은 없습니다. 컨텍스트는 AI 오케스트레이션의 보안 조치로 '데이터 암호화, 보안 API, 정기적인 보안 감사'를 언급하고 있으나, 이는 특정 '환경변수' 관리 방법을 지칭하지 않습니다....

**RCSV** (323c, 356.4ms): 환경변수를 안전하게 관리하는 방법은 다음과 같습니다:

1. **정기적인 모니터링**: 환경변수를 지속적으로 감시하여 이상 징후를 조기에 발견합니다.
2. **환경기준 준수**: 관련 법규 및 환경기준을 준수하여 오염을 예방합니다.
3. **위험 평가**: 환경영향평가를 실시하여 잠재적인 위험 요소를 사전에 파악합니다.
4. **오염 방지 대책**: 환경오...

## 5. 결론

### RAGAS 메트릭 승/패: HRKP 0 : 3 RCSV

**RCSV가 일부 메트릭에서 우세하지만, HRKP의 Graph RAG 기능은 별도의 가치를 제공합니다.**

### 각 시스템의 강점

| HRKP 강점 | RCSV 강점 |
|-----------|-----------|
| Knowledge Graph 기반 엔티티 검색 | OpenAI 고품질 임베딩 |
| RRF 3채널 퓨전 | GPT-4o-mini 답변 품질 |
| 로컬 임베딩 (비용 절감) | 간단한 아키텍처 |
| 관계 추론 (RELATED_TO) | 빠른 설정 |

---
*Generated: 2026-02-10 08:26 KST*
*Tool: scripts/rcsv_comparison_eval.py (STORY-111)*