# npm network resilience settings for Docker builds
# Prevents ETIMEDOUT errors in constrained network environments (WSL2/Docker)
fetch-timeout=120000
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
prefer-offline=true
